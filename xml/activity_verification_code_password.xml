<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content" >

    <EditText
        android:id="@+id/password_et"
        android:layout_width="@dimen/userLogin_edittext_width"
        android:layout_height="wrap_content"
        android:background="@color/user_login_text_hint_background"
        android:hint="@string/user_login_password"
        android:inputType="textPassword"
        android:textColor="@color/user_register_agree_color"
        android:textColorHint="@color/user_register_agree_color" />

    <ImageView
        android:layout_width="@dimen/userLogin_edittext_width"
        android:layout_height="wrap_content"
        android:layout_below="@+id/password_et"
        android:layout_marginTop="@dimen/userLogin_line_toEditText"
        android:background="@color/black_color" />

</RelativeLayout>